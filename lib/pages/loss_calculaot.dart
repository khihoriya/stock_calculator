import 'package:flutter/material.dart';

class loss_calculator extends StatefulWidget {
  const loss_calculator({Key? key}) : super(key: key);

  @override
  State<loss_calculator> createState() => _loss_calculatorState();
}

class _loss_calculatorState extends State<loss_calculator> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("LOSS"),);
  }
}
